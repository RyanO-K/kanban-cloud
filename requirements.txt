# Core server deps (SQLite fallback works with just these)
fastapi>=0.110
uvicorn>=0.29
sqlalchemy>=2.0
# Test deps
pytest>=8.0
httpx>=0.27

# Postgres/Neon driver (used when DATABASE_URL is set; harmless otherwise)
psycopg[binary]>=3.1
